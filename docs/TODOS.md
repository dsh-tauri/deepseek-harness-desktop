# Agent - 1

- 按照 `plugin.client.md` 重构 dsh-tauri-ui 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.host.md` 重构 dsh-tauri-ui 插件宿主端半区（Host Runtime）的架构。
- 按照 `plugin.host.service.md` 重构 dsh-tauri-ui 插件宿主端领域服务半区（Host Service Runtime）的架构。

# Agent - 2

- 按照 `plugin.client.md` 重构 dsh-tauri-worktree 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.host.md` 重构 dsh-tauri-worktree 插件宿主端半区（Host Runtime）的架构。
- 按照 `plugin.host.service.md` 重构 dsh-tauri-worktree 插件宿主端领域服务半区（Host Service Runtime）的架构。

# Agent - 3

- 按照 `plugin.client.md` 重构 dsh-tauri-panel-extension 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.client.panel.md` 重构 dsh-tauri-panel-extension 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.host.md` 重构 dsh-tauri-panel-extension 插件宿主端半区（Host Runtime）的架构。
- 按照 `plugin.host.service.md` 重构 dsh-tauri-panel-extension 插件宿主端领域服务半区（Host Service Runtime）的架构。

# Agent - 4

- 按照 `plugin.client.md` 重构 dsh-tauri-panel-scheduler 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.client.panel.md` 重构 dsh-tauri-panel-scheduler 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.host.md` 重构 dsh-tauri-panel-scheduler 插件宿主端半区（Host Runtime）的架构。
- 按照 `plugin.host.service.md` 重构 dsh-tauri-panel-scheduler 插件宿主端领域服务半区（Host Service Runtime）的架构。

# Agent - 5

- 按照 `plugin.client.md` 重构 dsh-tauri-pet 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.host.md` 重构 dsh-tauri-pet 插件宿主端半区（Host Runtime）的架构。
- 按照 `plugin.host.service.md` 重构 dsh-tauri-pet 插件宿主端领域服务半区（Host Service Runtime）的架构。

# Agent - 6

- 按照 `plugin.client.md` 重构 dsh-tauri-rightclick 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.host.md` 重构 dsh-tauri-rightclick 插件宿主端半区（Host Runtime）的架构。
- 按照 `plugin.host.service.md` 重构 dsh-tauri-rightclick 插件宿主端领域服务半区（Host Service Runtime）的架构。

# Agent - 7

- 按照 `plugin.client.md` 重构 dsh-tauri-session 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.host.md` 重构 dsh-tauri-session 插件宿主端半区（Host Runtime）的架构。
- 按照 `plugin.host.service.md` 重构 dsh-tauri-session 插件宿主端领域服务半区（Host Service Runtime）的架构。

# Agent - 8

- 按照 `plugin.client.md` 重构 dsh-tauri-turnrewind 插件客户端半区（Browser Runtime）的架构。
- 按照 `plugin.host.md` 重构 dsh-tauri-turnrewind 插件宿主端半区（Host Runtime）的架构。
- 按照 `plugin.host.service.md` 重构 dsh-tauri-turnrewind 插件宿主端领域服务半区（Host Service Runtime）的架构。
